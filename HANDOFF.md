# HANDOFF — smart-lab

**Write-only for Claude Code, read-only for the design session.** This is the one file Claude Code writes to — see `CLAUDE.md`'s "How this project is being run" for why (two writers on the same file was silently losing content; splitting reads from writes fixes that structurally).

**Claude Code:** whenever a task finishes, or you hit a blocker/open question mid-build, append an entry below — never edit `CLAUDE.md`, `SPEC.md`, or `TASK.md` yourself, and don't rewrite or delete earlier entries here, only add your own. Include:
- What you actually built (and the current app version reached — the live `APP_VERSION`, not just what `TASK.md` targeted).
- Any deviation from `TASK.md` or `SPEC.md`.
- Any real design/architecture decision you made along the way (schema shape, library choice, structure change).
- Any question or blocker, if you're stopping mid-task rather than completing it.

**Design session:** read this file, absorb anything durable into `SPEC.md`, and write the next `TASK.md`. **Never write to this file, not even to clear it** — doing that would reopen the exact two-writer collision this file exists to avoid, just in the other direction. Entries are dated, so what's new since the last check is whatever's dated after the last entry already absorbed into `SPEC.md`. If this file ever gets long enough to be unwieldy, that's Claude Code's call to make (e.g. moving old, already-absorbed entries into a dated archive file it also owns) — not the design session's.

---

- 2026-09-17 — Built all five steps on `docs/test/` only (per TASK.md's "Where to build this" — root untouched, still plain `0.3.1`). Current live version: `docs/test/` at `0.3.1-t01`.
  - **Step 1 (modularization)**: split the inline script into `docs/test/js/firebase-init.js` (app/auth/db instances, shared Firebase config, the `createAuthAccountWithoutSigningOut` secondary-app helper, `APP_VERSION`, `USERNAME_DOMAIN`), `auth-ui.js` (login/logout/change-password/greeting, imports `loadUserList` from admin-ui.js to trigger it post-login), `admin-ui.js` (add-user, user list, inline edit, reissue), and `update-banner.js`. `index.html` now just loads these four via plain `<script type="module" src="...">` tags — no bundler, as specified. Chosen boundaries match the task's suggested split exactly.
  - **Deviation flagged, not guessed around**: the update-checker used to regex-match `APP_VERSION` out of a fetched `./index.html`. Since `APP_VERSION` now lives in `firebase-init.js`, `index.html` no longer contains that string at all — the checker would have silently stopped working (fetch succeeds, regex match fails, function returns early, no error). Fixed by pointing the fetch at `./js/firebase-init.js` instead, since that's an unavoidable consequence of where the source of truth moved, not an ambiguous product decision. Verified in a real browser afterward (see below) — no console errors, and confirmed the modular load works end-to-end (admin screen, list, greeting all rendered correctly on a live authenticated session).
  - **Step 2 (offline persistence)**: `firebase-init.js`'s `db` now comes from `initializeFirestore(app, { localCache: persistentLocalCache({}) })` instead of `getFirestore(app)`. Initializes without error in a live browser check (no console errors on load or during admin-screen use). Did **not** run the suggested manual offline sanity check (DevTools network throttling, go offline, confirm the signed-in screen still behaves, reconnect) — flagging that as still open rather than claiming it's fully verified.
  - **Step 3**: removed the dead `ping`/`test_ping` match blocks from `setup/firestore.rules`. Left the `ping/latest`/`test_ping/latest` documents themselves alone, per the task's "not blocking" — up to the user to delete via Console if they want to. **Note**: this rules file is the single canonical copy shared by both builds (per an earlier decision) — the version now live in the repo needs to be re-published to the Firebase Console's Rules tab to actually take effect; I can't do that myself (no Firebase CLI in this environment).
  - **Step 4**: admin's greeting now reads "Hello **admin**" with no role suffix; confirmed visually in the live browser check. Every other role keeps "Hello **{username}** — {role}" (logic branches on `profile.role === 'admin'` in `auth-ui.js`'s new `renderGreeting()`).
  - **Step 5 (changelog)**: new `docs/test/js/changelog.js`. A `CHANGELOG` array (currently one entry, `0.3.1`) with plain-language notes; auto-popup compares `APP_VERSION` against a `smartlab_last_seen_version` `localStorage` key and shows everything newer, skipped entirely when `APP_VERSION` matches a `-tNN` pattern (regex `/-t\d+$/`) — confirmed in the live check that it did *not* auto-open on load for this `-t01` build. Clicking the version badge (now `cursor:pointer`) always shows the full list regardless of "seen" state — confirmed working via a real click in the browser (renders the `0.3.1` entry with both bullets, Close button dismisses it). Picking this up as a standing habit per `CLAUDE.md`'s new changelog convention, not just for this task.
  - **Browser verification performed**: loaded `https://matanyaa.github.io/smart-lab/test/` in an actual Chrome tab post-deploy (already signed in as `admin` from a prior session) — zero console errors on load, admin screen rendered correctly (user list, sliding Active/Non-active toggle, inline role/status editors, Reissue), greeting read "Hello **admin**" with no role suffix, changelog popup opened via direct click and via programmatic `.click()` (an automated-click coordinate miss made it look broken at first — a tooling artifact, not a bug, confirmed by dispatching the click programmatically instead).
  - **Not yet done**: promoting any of this to `docs/` (root) — task says only once confirmed.
- 2026-09-17 — **Task complete, handoff.** Current live version: `docs/test/` at `0.3.1-t01`, `docs/` (root) unchanged at plain `0.3.1`.
  - User republished `setup/firestore.rules` (with the dead `ping`/`test_ping` blocks removed) to the Firebase Console.
  - Finally closed out Iteration 3's long-open Step 5 verification, using real browser automation against the live `docs/test/` admin screen (not simulated): added three accounts through the actual add-user form (`verify_client`/client, `verify_leader`/team_leader, `verify_worker`/worker — verifier is gone from the model now, so that part of the original check no longer applies). Logged in as `verify_client`: greeting correctly read "Hello **verify_client** — client", admin screen correctly stayed hidden. **Rules genuinely enforce access, not just the UI**: while authenticated as `verify_client`, a direct Firestore SDK read of that account's own `users/{uid}` doc succeeded, and a read of a *different* user's doc (admin's) came back `permission-denied` — confirmed via a fresh, separately-authenticated Firebase app instance in the browser console, not just observing the UI hide a button.
  - **Reissue specifically not verified** — it's an admin-only action, and after logging out to test `verify_client`, getting back into the admin account needed its password, which I don't have and won't ask for or handle (not something I request from the user even to relay through chat — password entry has to happen directly in their own browser). Left the browser at the login screen with `admin` prefilled for the user to sign back into if they want to finish that specific check themselves; not blocking anything else.
  - **Leftover test data**: `verify_client`, `verify_leader`, `verify_worker` (and `test2` from earlier testing) are real, active accounts in the `users`/`usernames` collections now — harmless, but worth a mental note that the admin user list has test fixtures mixed into it. No delete feature exists (by design — see `SPEC.md`'s Authentication section); disabling them via the admin screen's status toggle is the available cleanup if wanted, left to the user's judgment rather than done unprompted.
  - Nothing else changed code-wise this session — purely a verification/testing pass on top of the previous entry's build.
- 2026-09-17 — User spotted "Hello admin — admin" still live on **root** (`docs/`) after the testing pass above — this task's Step 4 fix (drop the redundant role suffix for admin's own greeting) had only ever landed on `docs/test/`, since the whole task was scoped there pending a full promotion that hasn't happened yet. Patched root's still-unmodularized `index.html` directly with the same one-branch fix (`if (profile.role === 'admin')`) rather than waiting for that promotion, since it's small, self-contained, and low-risk. Root bumped `0.3.1` → `0.3.2` (plain, no `-tNN`, per the versioning convention for direct root edits) — icon cache-busting query strings updated to match. `docs/test/` unchanged, still `0.3.1-t01`. This is the second time a `docs/test/`-only fix has needed a matching root patch before the full promotion — worth the design session's attention if this pattern keeps recurring (root and `docs/test/` drifting on small UI details in between promotions).
- 2026-09-18 — **Iteration 4 built on `docs/test/`: core case/sample/action model.** Current live version: `docs/test/` at `0.4.0-t02`, root unchanged at `0.3.2`. New module `docs/test/js/cases.js`, wired into `auth-ui.js` via exported `showCasesScreen(profile)`/`hideCasesScreen()` (cases screen shown for admin/team_leader/worker, hidden for client — no client view exists yet, Phase plan item 5). All data model fields, stage transitions, and UI behavior built per TASK.md's spec. Real decisions/discoveries beyond what TASK.md spelled out:
  - **Firestore rules needed real design work TASK.md didn't specify.** Added `myProfile()`/`myRole()`/`myUsername()`/`isStaff()`/`isLeadOrAdmin()` helpers and `cases`/`samples`/`actions` match blocks: read = any staff (not client); case create = team_leader/admin; case update = team_leader/admin *or* the case's own `caseManager` (needed for day-to-day case-manager edits like `researchAddressed`); case/sample/action **delete = team_leader only, no admin exception** — the one place this iteration gets real rule enforcement rather than just UI hiding, matching TASK's explicit "not admin" for "Approve & delete" and treating it as the one truly destructive, cascading action worth a hard rule. Sample/action create+update = any staff (matches the "unclaimed to executed" loose model — no `assignedTo` field exists to restrict against anyway).
  - **`users/{uid}` read rule widened**: was self-or-admin only; picking a case manager needs a list of team_leader/worker usernames, which means any staff account now needs to read the whole `users` collection, not just their own doc. Usernames/roles aren't sensitive within the team, so this is a reasonable relaxation, not a workaround.
  - **"Publish report" and "Approve & delete" are team_leader-only in the UI, not team_leader-or-admin** — TASK.md says "(or admin)" explicitly for "Start lab" but not for these two, so I read that as deliberate rather than an oversight. Rules only *enforce* this strictly for delete (see above); "Publish report" is UI-only, matching the "loose enforcement" pattern already used for the on-hold/priority toggles and workflow ordering elsewhere in this iteration.
  - **Archiving workflow item schema** (not spelled out in TASK.md beyond "the same free-form action type"): `{ name, order, status, executedBy, executedAt }` — added `order` (a plain number, the parallel "Return parts"/"Archive samples" pair share the same value) purely for display sequencing, since TASK didn't give array items an explicit field for that. `executedAt` uses a plain client-side `Date`, not `serverTimestamp()` — the sentinel isn't supported inside array elements, only in top-level/map fields, so sample/case-level action *documents* (their own doc, not an array) use `serverTimestamp()` while archiving items (array elements on the case doc) use `new Date()` instead. Still stored as a real Firestore Timestamp either way.
  - **Cascade delete is manual, not automatic**: Firestore doesn't cascade-delete subcollections when a parent doc is deleted — "Approve & delete" enumerates every sample, then every action under each sample, deleting each individually before deleting the case doc itself. Verified this actually removes the whole subtree, not just the top-level doc (see verification below).
  - **"lab" → "writing" auto-advance is client-triggered, not server-triggered** — there's no Cloud Functions trigger on this project (staying off Blaze, per `SPEC.md`'s Authentication section), so the check ("are all of this case's samples' actions done?") runs client-side right after whichever action execution turns out to be the last one. **Bug found and fixed during testing**: if every action was already executed *before* "Start lab" was even clicked (a real possible order, not just a test artifact — nothing stops front-loading action execution during the `new` stage), the case would sit in `lab` forever, since the auto-advance check only ran as a side effect of executing an action. Fixed by also running the same check immediately after the "Start lab" transition itself (`0.4.0-t02`).
  - **Case number soft-validation** accepts `tempN` values as always-valid (no warning), on top of the 7-8-digit `YYMMNXX(X)` shape check for real numbers.
  - **Discovered, not caused by this task, but hit while testing it**: unlike `icon.svg`, the JS module files themselves (`firebase-init.js`, `cases.js`, etc.) aren't cache-busted with a `?v=` query string, only referenced as plain `<script type="module" src="js/....js">`. Within GitHub Pages' 10-minute `Cache-Control` window after a deploy, a plain reload can still execute stale module code even though the page's own HTML is fresh — I hit this directly testing the auto-advance fix (a normal navigate-reload still ran the old `t01` code; a hard refresh picked up `t02` correctly). The existing update-banner's *check* still works correctly regardless (its `fetch` uses `cache:'no-store'` explicitly), so users do get told a new version exists — but clicking "Reload" on that banner is a plain `location.reload()`, which isn't guaranteed to bypass the disk cache for the module files either. Not fixed here — cache-busting every cross-module `import` statement (not just top-level `<script src>` tags) to close this properly is nontrivial and easy to get out of sync on every version bump; flagging for the design session to weigh in on whether it's worth solving properly (e.g. a build-time-generated query param, or accepting occasional hard-refreshes as a known quirk of a no-bundler setup).
  - **Verification performed via real browser automation against the live `docs/test/` admin screen** (not simulated), across three seeded test accounts from a prior session (`verify_client`, `verify_leader`/team_leader, `verify_worker`):
    - Created a case, generated a temp number (`temp1`, correctly the lowest unused), confirmed the case-manager dropdown only offered team_leader/worker usernames (not admin, not client).
    - Added a sample with zones; confirmed all three default actions (Visual Inspection, Composition Analysis, Hardness Test) were seeded.
    - Executed all three actions *before* clicking "Start lab", confirmed stage correctly stayed `new` (not `lab`) throughout — then clicked "Start lab" and confirmed the fix above: jumped straight to `writing`, not stuck in `lab`.
    - Logged in as `verify_leader` (team_leader): confirmed "Publish report" is blocked with an inline message while `researchAddressed` is false, and enabled once toggled true (toggled as admin first, confirming any staff whose role/caseManager-match allows it can flip that specific flag). Published → reached `archiving`. Marked all 5 seeded archiving-workflow items done via "Mark done" (only offered in the `archiving` stage, confirmed absent in `writing`/`new`).
    - Clicked "Approve & delete" as `verify_leader`: confirmed the inline two-step confirm pattern (not a native `confirm()` dialog, consistent with the rest of the app), then confirmed the case and **both its samples and all their actions** were actually gone from Firestore afterward (queried directly, not just observed the UI clear) — the cascade delete genuinely works, not just at the top level.
    - Directly confirmed rules enforcement, not just UI hiding: authenticated as `verify_worker` (a plain `worker`) via a separate script-created Firebase app instance, attempted `deleteDoc` on a case → `permission-denied` as expected; attempted `addDoc` on that same case's `samples` subcollection → succeeded, as expected for the broader staff-write rule.
  - **Not yet done**: promoting any of this to `docs/` (root) — task says only once confirmed, and this is a much bigger feature than the last couple of root patches, so I'd want your explicit go-ahead before touching root at all this time.
- 2026-09-18 — Two ad hoc follow-ups on `docs/test/`, at the user's request, not yet re-verified by me (user asked to test the first one themselves rather than have me do it via browser automation this time):
  - **Admin no longer sees the Cases screen** — narrowed back to admin's role being user-management only. `auth-ui.js`'s role check for showing Cases is now `['team_leader', 'worker']`, dropping `admin`. Note: this is a UI-only change — `setup/firestore.rules`' underlying case/sample/action permissions for admin (`isLeadOrAdmin()`, etc.) are untouched, so admin still has the *capability* at the rules layer, just no screen surfacing it right now. Flagging in case that split (UI hidden, rules still open) is or isn't what's wanted going forward.
  - **Admin can now delete a user's profile doc directly**, and **reissue deletes the old account's profile automatically** instead of leaving it `disabled`. Required a real rules change: `users/{uid}`'s `allow delete` went from hard `false` to `isAdmin()`. Important limitation stated directly to the user and worth repeating here: this deletes the Firestore **profile** doc, not the underlying Firebase Auth **credential** — true Auth-account deletion still needs the Admin SDK (Blaze), same constraint that motivated reissue's design in the first place. Deleting the profile doc is still a real, immediate lockout in practice, since every rule keys off that doc's existence, but the Firebase Console's Authentication tab may now show orphaned credentials with no matching app profile — expected, not a bug, and only cleanable via the Console itself or eventually Blaze.
  - **Removed the Active/Non-active toggle** from the admin user list entirely — with delete now handling cleanup directly, there's no growing disabled-list clutter left to filter away, so the list just shows everyone, active or disabled, in one flat view again (reverting the collection-and-render split from `renderUserList()`/`allUsers` back to a plain full-list render, though the underlying `allUsers` caching pattern from `loadUserList()` stayed since other code still benefits from it).
  - Current live version: `docs/test/` at `0.4.0-t04`. Root still untouched at `0.3.2`.
- 2026-09-18 — **Firestore rules auto-deploy built (repo tooling, no `docs/`/`docs/test/`/`APP_VERSION` change).** `setup/firebase.json` (scoped to `firestore.rules` only) and `.github/workflows/deploy-firestore-rules.yml` (triggers on push to `main` touching `setup/firestore.rules`), exactly per spec.
  - **Real values confirmed, not guessed**: project ID `smart-lab-abd80` (found in `docs/index.html` and `docs/test/js/firebase-init.js` — see deviation below), deploy branch `main` (confirmed both as the current branch and GitHub's actual default/HEAD branch via the API, not assumed).
  - **Deviation**: TASK.md's step 2 said to find the project ID in `docs/js/firebase-init.js` "after the Iteration 3 modularization" — that path doesn't exist. Root (`docs/`) was never actually modularized or promoted; only `docs/test/` was, per every entry above this one. Not a blocker (TASK.md's own "Deviations / open questions" section allows for this) — found the same ID in `docs/index.html`'s still-inline config instead.
  - **The workflow file itself needed the user's explicit go-ahead to commit** — Claude Code's own safety tooling blocked `git add` on anything under `.github/workflows/` until asked directly, treating a workflow file as higher-stakes than ordinary code (it runs automatically with repo secret access). Worth knowing about for any future task that touches `.github/workflows/` — it's not something I can route around, and re-asking each time is expected, not a bug in this task specifically.
  - **End-to-end test run for real, not just reasoned about** (TASK.md's step 4): pushed a genuinely whitespace-only diff (one trailing blank line) to `setup/firestore.rules` on `main`, then confirmed via GitHub's public Actions API (no `gh` CLI or auth token available in this environment, so used `curl` against `api.github.com` directly) that "Deploy Firestore Rules" triggered immediately on that push. Outcome: `checkout` and `Write service account credentials` steps both succeeded, then `Deploy Firestore rules` itself failed — the job-level result matches TASK.md's own predicted "expected, not a bug" outcome (auth failure from the `FIREBASE_SERVICE_ACCOUNT` secret not existing yet), and the failure landing specifically on the deploy step rather than checkout/setup is consistent with that reading. Couldn't pull the actual log text to see the literal error message and fully confirm it's an auth failure specifically (not some other misconfiguration) — GitHub's job-logs API endpoint returned `403 Must have admin rights to Repository` for an unauthenticated request, and no `gh`/token is available here. **Worth a direct look at the Actions tab** (run: `https://github.com/Matanyaa/smart-lab/actions/runs/35322218404`) to confirm the exact failure reason before assuming this is purely "secret not set up yet" versus a real bug in the workflow.
  - Once `FIREBASE_SERVICE_ACCOUNT` is added as a repo secret (the user's manual step, per TASK.md — can't be done from inside this repo), any future `setup/firestore.rules` change pushed to `main` should deploy automatically, closing the gap that caused a rules-publish to be missed once already.
- 2026-09-18 — **Removed the `status` field from `users` entirely, at the user's request** (unrelated to the Actions task above, done in the same session). `docs/test/`: add-user no longer writes `status`, the admin user-list table dropped its Status column entirely (colspan values updated 4→3 throughout `admin-ui.js`), and reissue's replacement-profile `setDoc` no longer sets it either. `setup/firestore.rules` never referenced `status` in any rule, so no rules change was needed. Existing Firestore documents that still carry a stale `status` field are harmless leftovers, not migrated — nothing reads or writes that field anymore. Current live version: `docs/test/` at `0.4.0-t05`.
- 2026-09-18 — **Bug fix, found by the user testing the admin Delete button directly**: Delete only removed the `users/{uid}` doc, not the matching `usernames/{username}` doc — so a deleted user's `usernames` entry stayed behind in Firestore, still pointing at the (now gone) account, looking like "the user is still there" even though the app's own list correctly showed them gone. Fixed: Delete now removes both docs. No rules change needed — `usernames/{username}`'s existing `allow write: if isAdmin();` already covers delete (Firestore's `write` shorthand includes create/update/delete together). Also added error handling to the delete-confirm click, which previously had no try/catch and would fail completely silently. **Note for the user**: this fix only prevents it going forward — any account you already deleted with the old buggy button (before `0.4.0-t06`) will still have an orphaned `usernames/{that-username}` doc sitting in Firestore; worth a quick manual check/cleanup in the Console for whichever account you tested with. Current live version: `docs/test/` at `0.4.0-t06`.
- 2026-09-18 — **Bug fix, reported by the user**: clicking "Reload" on the update-available banner could still leave the old version running — confirmed as a caching issue, since opening the same URL in incognito correctly showed the new version while a normal reload didn't. Root cause: this was the exact gap flagged (but left unfixed) in an earlier entry above — neither `index.html` nor the `js/*.js` module files are cache-busted with a `?v=` query string the way `icon.svg` is, so within GitHub Pages' 10-minute `Cache-Control` window, a plain `location.reload()` isn't guaranteed to bypass the browser's disk cache for any of them. Fixed properly this time: the Reload button now force-refetches `index.html` and every `<script type="module" src="...">` on the page (read from the DOM directly, so it stays correct if more modules get added later, no hardcoded list to keep in sync) using `fetch(url, {cache: 'reload'})` — that mode bypasses reading the existing cache entry but still writes the fresh response into it — *before* actually calling `location.reload()`, so the reload's own resource loads pick up what was just fetched instead of whatever was cached before. Verified all six URLs (`index.html` + 5 module files) fetch cleanly with this approach via direct browser testing; couldn't fully reproduce the original stale-cache symptom itself in that same session (needs a live 10-minute cache window to trigger naturally, not something reproducible on demand), so this is confirmed *correct by construction* (standard, well-defined Fetch API cache mode) rather than confirmed by reproducing-then-fixing the exact failure — worth the user confirming next time the banner actually appears for them. Current live version: `docs/test/` at `0.4.0-t07`.
- 2026-09-18 — At the user's request: admin's own row in the user list no longer shows a password-reset action at all (admin can already self-service via the existing "Change password" button), and "Reissue" is renamed to "Reset password" everywhere (button text, and `admin-ui.js`'s `reissueUser` function renamed to `resetUserPassword`) since that's the clearer user-facing term for what's really happening to someone *else's* account. Both `resetPasswordBtn` and `deleteBtn` element creation moved inside the existing `if (u.role !== 'admin')` branch in `renderUserRow()` — previously only the role-dropdown/Delete swap was gated on that check, but the reset-password button was created unconditionally for every row including admin's own. Live-verified visually afterward (see next entry) — admin's row shows only username + role, no action buttons; every other row shows "Reset password" and "Delete". Current live version: `docs/test/` at `0.4.0-t08`.
- 2026-09-18 — **Correction to the previous `0.4.0-t07` Reload entry above: that fix did not actually work.** The user asked to verify it, and live testing (a real stale tab, genuinely holding `t07`, with `t08` already deployed) showed clicking Reload left the page on `t07` — the `fetch(url, {cache:'reload'})` pre-fetch, despite being a standards-defined cache mode that's supposed to write a fresh response into the cache before the reload, did not reliably do so here (network log showed all pre-fetches returning 200, but the reloaded page's own module content stayed stale regardless). Noting this clearly since the earlier entry's "confirmed correct by construction" framing turned out to be misplaced confidence — a mechanism being spec-correct isn't the same as it working against this specific host (GitHub Pages behind Fastly).
  - **Real fix, this time verified by actually reproducing and then clearing the failure, not just reasoned about**: every module file is now loaded through a version-stamped URL — `index.html`'s five `<script type="module" src="js/....js?v=<version>">` tags, and every local cross-import between the modules themselves (`auth-ui.js` → `firebase-init.js`/`admin-ui.js`/`cases.js`; `admin-ui.js`, `cases.js`, `update-banner.js`, `changelog.js` → `firebase-init.js`) now carry the identical `?v=<version>` query string. This is the exact cache-busting approach already proven to work for `icon.svg` — a different URL is a guaranteed cache miss, sidestepping cache-mode semantics entirely rather than trying to force a particular one to behave a particular way. The Reload button itself no longer does any pre-fetching — it just navigates to `location.pathname + '?_r=' + Date.now()`, a throwaway cache-busting query on the top-level page URL, to force a fresh `index.html` fetch too (which is *not* itself version-stamped, since it's the one file that has to be reachable at a stable bare URL).
  - **Real maintenance cost, stated plainly**: every module's cross-import line, plus `index.html`'s five script tags plus its two `icon.svg` references, all need their `?v=` bumped together on every version change — six files, roughly a dozen lines. `firebase-init.js`'s own `APP_VERSION` comment now says so explicitly, as a per-bump checklist. This is more surface than the old single-constant bump, but it's the tradeoff for a mechanism proven to actually work here versus one that looked correct on paper and wasn't.
  - **Verified against a real reproduction of the original bug, twice**: first pass — loaded a tab on `t09` while `t10` was already live (a genuinely stale tab, not simulated), clicked Reload, confirmed via the URL bar (`?_r=...` present) and a zoomed screenshot of the version badge that the tab actually moved to `t10`. A follow-up automation-session boundary lost that browser tab mid-test, so repeated the exact same test end-to-end a second time (`t10` → `t11`) to be sure the first result wasn't a fluke — same outcome, badge correctly read `v0.4.0-t11` after Reload. Also re-confirmed visually in this same pass that the previous entry's admin-row change is correct: admin's row has no Reset password/Delete buttons, `client`/`lead`/`work` rows have both.
  - Current live version: `docs/test/` at `0.4.0-t11` (three throwaway version-only bumps — `t09` shipped the real fix, `t10`/`t11` had no code changes beyond the version stamps themselves, needed purely to get a genuinely stale tab to test against without waiting on GitHub Pages' natural cache window). Root still untouched at `0.3.2`.
- 2026-09-18 — **0.4.1 built on `docs/test/` (`0.4.1-t01`): case model & case view refinements**, per `TASK.md`'s ten items. Root untouched, still `0.3.2`. All ten items built; two are only partially verified live — see the blockers at the end of this entry.
  - **New Firestore collections**: `caseTypes/{id}` (`name`, `tatGoalDays`, `labWorkflowTemplate`, `archivingWorkflowTemplate`), `clientOrgs/{id}` (`name`), `clients/{id}` (`name`, `clientOrgId`). New admin screens for both (`docs/test/js/case-types-ui.js`, `clients-ui.js`), added as tabs alongside the existing Users panel on the admin screen.
  - **Case type workflow templates — a real simplification, not the full design**: TASK's case type CRUD doesn't spell out a template *editor*, and building one (a full nested-workflow UI) felt like real scope creep for this pass. Instead, "Add case type" has a single "Seed default workflow" checkbox that, if checked, populates `labWorkflowTemplate`/`archivingWorkflowTemplate` with the existing hardcoded `failure analysis` defaults (Visual Inspection → Composition Analysis ‖ Hardness Test; Publish → Close-in-access → (Return parts ‖ Archive samples) → Scan); unchecked, both arrays stay empty, matching SPEC's "a case type with no template gives its cases an empty case workflow to build manually." This satisfies TASK's "seed one case type for this pass: failure analysis" requirement without a template editor. Worth the design session's attention if per-case-type custom templates become a real near-term need.
  - **`clientOrgId` added to `users/{uid}`** for `client`-role accounts (not in the original Iteration 3 schema) — a client login is a shared login for exactly one org, so it needs to know which one. Set via a new "Client org" dropdown on the add-user form, shown only when role=`client` is picked; also carried forward correctly on reissue/reset-password (the old code copied only `username`/`role`, which would have silently dropped a client account's org link on its first password reset — fixed as part of this same change, not a separately-reported bug).
  - **Rules redesign** (`setup/firestore.rules`): new `isCaseStaff()` (`team_leader`/`worker` only) replaces `isStaff()`/`isLeadOrAdmin()` everywhere under `cases`/`samples`/`actions` — admin now has zero rule-level access to any of it, not even read (closing the gap flagged in the entry above). Case `create` requires `myRole() == 'team_leader'` *and* `request.resource.data.openedBy == myUsername()` — enforced server-side, not just trusted from the client. New `clientBelongsToMyOrg()` helper backs a client-role read rule on `cases`: `isCaseStaff() || (myRole() == 'client' && clientBelongsToMyOrg(resource.data.client))`. New `caseTypes`/`clientOrgs`/`clients` collections: read = any signed-in user (org/client names aren't sensitive, matching the existing usernames/roles reasoning) except `caseTypes` which is staff-only (`isStaff()`, admin included, since admin still needs read to manage them and only staff need it for case creation); write = admin-only on all three. New `notes` subcollection under `cases/{caseId}` mirrors the `samples`/`actions` pattern (`isCaseStaff()` read/create/update, `team_leader`-only delete).
  - **A real correctness gap, handled deliberately rather than papered over**: case `update` stays `team_leader`-or-`caseManager`-only (unchanged from Iteration 4, per TASK's rules section #9) — but stage auto-advances (`lab`→`write`, `archive`→`done`) are triggered as a side effect of *any* staff member's action (e.g. a plain worker who isn't the case's manager finishing the last action). Under the unchanged rule, that worker's own attempt to write the case-level stage field would be denied. Rather than widen the rule (which the task didn't ask for and would weaken the caseManager-scoping intent), auto-advances now check `canUpdateCase()` client-side first and skip silently if the current user isn't allowed to write the case doc — the transition then happens opportunistically the next time an authorized user (the team leader or case manager) opens the case, since `renderCaseDetail` re-runs the same checks on every view. Same pattern applied to the `write`→`lab` reversibility rule (reopening an action / adding a sample or action after auto-advancing to `write`). This was already a latent, unfixed gap in Iteration 4's original code (same rule shape existed then) — not something 0.4.1 introduced, just newly load-bearing now that verified-type actions and a real writing workflow make the multi-stage transition chain more visible.
  - **Case workflow**: `stage` is a plain field (`new`/`lab`/`write`/`archive`/`done`), not a generic recursive workflow object — SPEC.md frames the case workflow as "the same recursive workflow primitive," but also says this top-level sequence "is not user-customizable," so modeling it as a real recursive structure would have added machinery with no behavioral payoff for this pass. `archivingWorkflow` (already a plain array field from Iteration 4) and the new `labWorkflowTemplate` snapshot on the case *are* the actual case-type-seeded content; `stage` itself is just an enum. Logged as a simplification, not a deviation from anything actually required.
  - **Writing workflow**: implemented exactly as SPEC's provisional simplification describes — `writingStage` (`draft`/`leaderReview`/`secondDraft`/`orgManagerReview`/`published`), each review's "back" is a plain single-step return (leaderReview→draft, orgManagerReview→secondDraft — resolving the explicitly-unconfirmed "does org-manager-review's back land on secondDraft" question as yes, per SPEC's own "assumed same as leader review's back"), no revision counter, no cap. Publishing sets `writingStage:'published'` and `stage:'archive'` in the same update. Who can drive these transitions: gated the same as any other case-level update (`canUpdateCase()` — team_leader or the case's own manager), not further restricted per SPEC's silence on exactly who reviews.
  - **Verifier**: implemented per SPEC's working assumption — any `isCaseStaff()` account other than `executedBy`, checked client-side (`a.executedBy !== myProfile.username`) before showing the Verify control; enforced at the rules layer only as broadly as "any case staff can write this action," same as execution itself (no rules-level check that verifier ≠ executor — that would need comparing `request.auth`'s username against the existing doc's `executedBy` field in a rule, which is possible but wasn't built this pass since SPEC frames this as a UI-level convention, not a hard security boundary. Flagging in case that's wanted later.)
  - **Actions**: full new schema built as specified (`type`, `environment`/`calibration`/`measurements`/`verifiedMeasurements` as `{name, actualValue, units}` lists via a small reusable row-editor, `status` boolean, `executedBy`/`executionTimestamp`, `verifiedBy`/`verificationTimestamp`). `isActionComplete(a)` = `status===true && (type!=='verified' || !!verifiedBy)` gates both the `lab`→`write` auto-advance and the per-sample done-count display. "Reopen" (any case staff) resets an action back to unexecuted/unverified and, if the case had already reached `write`, pulls it back to `lab` per SPEC's reversibility rule.
  - **Notes**: flat `cases/{caseId}/notes/{noteId}` subcollection with a `parentId` field, built into a tree client-side rather than true nested subcollections — simpler to query and delete-cascade, and nesting depth in practice is expected to be shallow (a note plus a few replies), so a flat-with-parent-pointer model was a reasonable simplification over recursive subcollections. Deleted along with the rest of the case's data on cascade delete.
  - **Client-facing view** (`docs/test/js/client-view.js`, new): minimal per TASK's explicit allowance — a filtered case list (org-wide, or narrowed to one client via a dropdown) showing `oname` + status text (`"In lab"` for `new`/`lab`, `"Writing"` for `write`, invisible from `archive` on — matching SPEC exactly) plus on-hold/priority badges and a "(research ongoing)" suffix when `showResearchToClient` is set and the case is still visible. Rules-scoped via `clientBelongsToMyOrg`, not just UI-filtered. **Known limitation, not built**: the client-side `client` `in` query caps at 30 values, so a client org with more than 30 named clients would silently miss cases for the overflow — fine at this project's current scale, would need chunked queries if that ever changes.
  - **"+ New case" toggle fix** (TASK item #10): now flips visibly between "+ New case" and "✕ Cancel" with a pressed/`active` style, instead of the old static label.
  - **Case deletion**: available at any stage now (removed the old archive-stage-only gating), relabeled from "Approve & delete" to plain "Delete case" since it's no longer an approval gate tied to workflow completion — just a standalone team-leader judgment call, per SPEC.
  - **Syntax-checked all new/changed JS with `node --check` before deploying** (no Firebase CLI auth available in this environment to do a real rules dry-run — see the blocker below); all files parsed cleanly.
  - **Blocker 1 — Firestore rules auto-deploy failed again, same as the last recorded attempt**: the GitHub Actions run for this push (`Deploy Firestore Rules`) shows the same job-step shape as the earlier documented failure (`checkout` and `Write service account credentials` succeed, `Deploy Firestore rules` fails) — consistent with `FIREBASE_SERVICE_ACCOUNT` still not being configured as a repo secret, not a new bug, though the exact log text still can't be pulled (same `403` on unauthenticated job-log access as before). **This means the new rules are very likely NOT yet live in Firebase** as of this entry — admin may still have old, broader case-data access, and the new `caseTypes`/`clientOrgs`/`clients` collections have no rules at all until this deploys, which would deny all access to them (Firestore denies by default). **The user needs to either add that repo secret, or manually publish `setup/firestore.rules` via the Firebase Console (the same one-time step used before Actions was automated)** before the rules-dependent parts of this build (case type/client picker, client login view, admin lockout) will actually work correctly.
  - **Blocker 2 — full cross-account live verification wasn't completed.** Verified successfully: case creation without a case manager works; the "+ New case" toggle behaves correctly; a `verified`-type action correctly shows "(awaiting verification by someone else)" and offers no Verify control when the signed-in account (`lead`, a pre-existing authenticated session) is the same as `executedBy`; the `lab` stage's progress text correctly withholds credit for an executed-but-unverified action; `Start lab`/stage display all render correctly. **Not completed**: actually signing in as a *different* account to perform the verification, testing the client-org login's scoping, and confirming admin's rules-level lockout with a real second authenticated session — all of these need typing a password into either the login form or the admin add-user form, which this session's browser-automation policy prohibits outright (no password entry into any field, including for disposable test fixtures). Earlier iterations' testing passes did type passwords for test accounts; this session's tooling explicitly disallows it now. **This needs the user's help**: either they enter a test account's password directly when I've navigated to the right screen, or they run through this specific scenario themselves and report back. Logged here rather than silently claiming full verification.
  - Current live version: `docs/test/` at `0.4.1-t01`. Root untouched at `0.3.2`.
- 2026-09-19 — **Mobile UI iteration, round 1 (header/settings redesign), at the user's direct request** (main use of the app is on a phone). A first mobile-CSS-only pass (zoom prevention, table overflow safety, tap targets) was built, then explicitly reverted by the user via `git revert` before they gave specific instructions instead — that revert is commit `08d5a1b`, restoring `0.4.1-t01`'s CSS exactly. This entry is the follow-up pass built to their specific instructions, reaching `0.4.1-t02`:
  - **Logo gesture, new**: right-click (desktop) or long-press ~600ms (phone) on the header logo navigates from `docs/test/` to the launched app at `docs/` (`location.href = '../'`). Deliberately not the ordinary click/tap, so it can't be triggered by accident. Verified live: right-clicking the logo on `docs/test/` correctly loaded root's own (older, `0.3.2`) UI. No reverse gesture built on root — not asked for, and root's `index.html` is a separate, non-modularized file this task didn't touch.
  - **Version display moved**: was an inline pill badge next to "smart-lab"; now small dim text stacked directly under the app name (`.version-sub`), keeping its existing click-to-reopen-changelog behavior (`changelog.js` untouched, still targets `#versionBadge` by id).
  - **Greeting simplified**: dropped the "Hello" prefix and changed the separator from "name — role" (em dash) to "name | role" (matching the pipe-delimited style already used for case titles elsewhere in the app). Admin still shows just its username with no role suffix, unchanged from before (name and role are the same word for that one account).
  - **Settings screen, new**: "Change password" moved off the header (was a `showChangePasswordBtn` text button toggling a `changePasswordScreen` panel) into a new `settingsScreen` panel titled "Settings" with "Change password" as a labeled subsection inside it — a natural home for any future account-level settings, not just this one form. Reached via a new gear-icon button (`settingsBtn`), replacing the old text button.
  - **Log out is now icon-only**: same button/behavior, just an exit-arrow SVG instead of the text "Log out". Placed together with the new settings gear button, top-right of the header.
  - **Icons are small custom-drawn inline SVGs** (a radiating-tick "settings" glyph, a door-and-arrow "log out" glyph) rather than pulling in an icon font/library, consistent with this project's no-build-step, no-dependencies approach. Both buttons carry `title`/`aria-label` text for accessibility since they're otherwise visual-only.
  - **Verified live via real browser automation**: greeting reads "lead | team_leader" (no "Hello"); version sits under the app name in small text; both header icon buttons render and are clickable; clicking the gear opens the Settings screen showing the Change-password form correctly; right-clicking the logo genuinely navigated to root's live app. Console clean (only the pre-existing, harmless Firestore multi-tab persistence warning).
  - **Not yet done**: the user said this is round 1 of an ongoing mobile-UI iteration ("I will give you instructions for what to do") — no further mobile layout work (the tables/tap-targets/zoom-prevention concerns from the reverted first pass) has been redone yet, pending their next round of specific instructions.
  - Current live version: `docs/test/` at `0.4.1-t02`. Root untouched at `0.3.2`.
- 2026-09-19 — **Launched: root (`docs/`) promoted from `0.3.2` straight to `0.4.1`, at the user's direct request ("update the launched app").** This is root's first real update since Iteration 3, and the first time root gets modularized into separate `js/` files the way `docs/test/` already was — root had never received Iteration 4 or 0.4.1 before now.
  - **Not a byte-for-byte copy** — root deliberately keeps several things `docs/test/` doesn't have or doesn't need: its own `icon.svg`/`manifest.json`/`sw.js` (untouched), the `<link rel="manifest">`/`apple-touch-icon` tags and service-worker registration script, and its own long-standing **green** accent color (`#4ac98f`/`#2f7a58`) rather than picking up `docs/test/`'s orange (`#f5a623`/`#a86a17`) — these two colors appear to be a deliberate, pre-existing visual distinguisher between the launched app and the in-progress test build, so root's was preserved rather than overwritten. `<title>` changed from "Smart Lab (Test)" to plain "Smart Lab". Version strings dropped their `-tNN` suffix (`0.4.1-t02` → `0.4.1`) per the versioning convention for root. The `docs/test/`-only "right-click/long-press the logo to jump to the launched app" gesture was stripped out of root's copy of `auth-ui.js` — it has no meaningful destination from root and was never asked for there.
  - **`setup/firestore.rules` needed no changes** for this promotion — it's one shared file already covering both environments (single Firebase project), already updated as part of the 0.4.1 work itself.
  - **Flagged before promoting, not silently handled**: `users`/`usernames` have been shared between root and `docs/test/` since Iteration 3 by explicit prior design (documented in this file's own history), and it turns out the newer `cases`/`samples`/`actions`/`caseTypes`/`clientOrgs`/`clients` collections were never given `docs/test/`'s `test_` prefix treatment either — they're the *same* collections in both environments too. Checked the live `docs/test/` case list before promoting: one case exists ("CaseNum - ClientNum - TestName | work | day 1 | 2026-10-17 | New") that doesn't match any of my own test fixtures from this session, so it's very likely something the user created themselves while trying out the test build. **Deliberately left untouched, not deleted** — I don't delete data I don't know the origin or intent of; the user can remove it themselves post-promotion if it's not wanted. Worth the design session's attention: if real case data is going to accumulate on root from here on, the collection-sharing between root and `docs/test/` (fine for user accounts, per the original design) may be worth revisiting for cases specifically, since test-build experimentation and real lab work would otherwise land in the same collection.
  - **Verified via read-only checks only, not live browser interaction** — per this file's own standing rule ("Never test against the launched app," added 2026-09-18), root was never opened or clicked through. Confirmed instead via `curl`: `js/firebase-init.js` serves `APP_VERSION = '0.4.1'`, and `index.html`, `manifest.json`, `sw.js`, `icon.svg`, and all eight `js/*.js` module files all return `200`.
  - Current live version: `docs/` (root) at `0.4.1`, live at `https://matanyaa.github.io/smart-lab/`. `docs/test/` unchanged, still `0.4.1-t02`.
- 2026-09-19 — **Root patch: reverse logo gesture, at the user's direct request.** Mirrors `docs/test/`'s own gesture in the opposite direction — right-click (desktop) / long-press (phone) on the header logo now jumps from the launched app to `docs/test/` (`location.href = './test/'`). Direct root patch (not proven out in `docs/test/` first) since it's trivially simple and only makes sense on root anyway. Root bumped `0.4.1` → `0.4.2` (plain, no `-tNN`, per convention for a direct root edit) — all `?v=` query strings and `APP_VERSION` updated to match.
  - **Caught and fixed a mistake before committing**: the version-bump `sed` command was run from inside `docs/` with a recursive `.` pattern, which also matched and corrupted `docs/test/`'s own version strings (mangled into a nonsensical `0.4.2-t02`). Caught by reviewing `git status`/`git diff` before staging — `docs/test/`'s files were reverted with `git checkout --` before anything was committed, so `docs/test/` was never actually affected; only `docs/` (root) carries this change.
  - **Verified via read-only checks only** (same standing rule as the promotion above): confirmed via `curl` that `js/firebase-init.js` serves `APP_VERSION = '0.4.2'` and that the deployed `js/auth-ui.js` contains the new `location.href = './test/'` gesture code. No live browser interaction with root.
  - Current live version: `docs/` (root) at `0.4.2`. `docs/test/` unchanged, still `0.4.1-t02`.
- 2026-09-19 — **Mobile UI iteration round 3, `docs/test/` only.** The user asked to revert round 2 (the centered-logo/3-column-grid header) and rebuild the stacked-greeting idea differently: keep name-over-role, but instead of centering the logo, make it bigger and sit beside a two-line text block (like a typical app icon next to a two-line title/subtitle), staying left-aligned.
  - **Reverted round 2 cleanly**: used `git revert` (not `reset`) on both its commits (header change + its HANDOFF entry), restoring `0.4.1-t02`'s exact header. Caught my own mistake here before it caused damage: I first tried `git reset --soft` to squash the two revert commits into one for a tidier message, but ran it against a commit that was already on `origin/main` — that would have left local `main` behind the pushed history, requiring a force-push to reconcile (never done that; caught it via `git log`/`git status` before pushing anything). Fixed by using the reflog to recover the proper forward-only revert commits and `git reset --soft` only as far back as the actual current pushed tip (safe, since nothing before that point was touched) — squashed correctly into one clean revert commit and pushed as an ordinary fast-forward, no force needed, no pushed history rewritten.
  - **Rebuilt from the reverted state**: `.brand-block` changed from a single column (icon inline with the "smart-lab" text, version below) to a row — a plain `<img>` (bumped from 22px to 36px) beside a new `.brand-text` column holding the existing `<h1>smart-lab</h1>` + version line, so the bigger icon visually spans both lines. `renderGreeting()` in `auth-ui.js` rebuilt the same way as round 2's version (two stacked `<span>`s, `.greeting-name` + reused `.version-sub`), but the header itself stayed a plain left-to-right `flex` row (not round 2's `grid`), so the logo/greeting/action-buttons layout reads left-to-right again instead of centered.
  - **Verified live via browser automation**: a stale cached tab correctly showed the update banner, and after clicking Reload landed on `v0.4.1-t04` with the bigger icon sitting beside the two-line "smart-lab / v0.4.1-t04" text and "lead / team_leader" stacked to its right — console clean apart from the known harmless Firestore multi-tab warning.
  - Current live version: `docs/test/` at `0.4.1-t04`. Root untouched at `0.4.2`.
- 2026-09-19 — **Mobile UI iteration round 4, `docs/test/` only, small follow-up.** Center-aligned the stacked name/role greeting text (`.greeting{align-items:center; text-align:center;}`) — previously left-aligned, so the shorter line (e.g. "lead") sat flush against the longer one's ("team_leader") left edge; now the shorter line centers above/below the wider one. Verified live: console clean (same harmless Firestore warning as always), "lead" visibly centered above "team_leader".
  - Current live version: `docs/test/` at `0.4.1-t05`. Root untouched at `0.4.2`.
- 2026-09-19 — **Mobile UI iteration round 5, `docs/test/` only.** Round 4's centering only centered the greeting's own two lines relative to each other — the block itself still sat flush against the logo's right edge, not centered in the actual leftover space between the logo and the action icons. Fixed with `header{display:grid; grid-template-columns:auto 1fr auto;}` (logo left, greeting centered in the flexible middle column, action icons right) — same grid technique round 2 used to center the logo, just applied to the greeting instead this time, with the logo staying put on the left as round 3 settled on. Verified live: reloaded a stale tab via the update banner, landed on `v0.4.1-t06` with "lead / team_leader" now genuinely centered in the gap between the logo and the settings/logout icons.
  - Current live version: `docs/test/` at `0.4.1-t06`. Root untouched at `0.4.2`.
- 2026-09-19 — **Cases screen redesigned at the user's request, `docs/test/` only: grouped by case type into cards, numbered two-line rows.** Previously one flat list under a plain "Cases" heading; now cases are grouped by their `caseType`, each type getting its own card titled with the type's own name (e.g. "Failure Analysis") instead of a generic label — cases with no case type land in their own "No case type" card, sorted after the named ones. Dropped `<table class="case-list">`/its "Case" column header for the team-facing screen entirely in favor of plain numbered `.case-row-item` divs: a bold top line ("N. caseNumber - clientCaseNumber - name", `onameOf()` prefixed with a running number that restarts per card) and a bottom line split via flex `justify-content:space-between` — day-count/due-date stacked on the left (reusing the `.version-sub` small-text style from the header), stage/case-manager ("New | work") on the right. On-hold/priority badges moved inline onto the top line. The single-line `caseTitle()` helper (`oname | manager | day | due | status`) is untouched and still used for the case *detail* screen's header text — this change was scoped to the list view only, per the request. **The client-facing view (`client-view.js`) was deliberately left alone** — it keeps its own simpler flat table, which SPEC.md already frames as intentionally minimal for that audience, not something this request was asking to touch.
  - **Verified live via browser automation**: the one existing test-build case (client "Hak — Hok1", case type "Failure Analysis" — clearly the user's own test data, not one of my fixtures) rendered correctly grouped under a "Failure Analysis" card, numbered "1. CaseNum - ClientNum - TestName", with "day 1 / 2026-10-17" on the bottom-left and "New | work" on the bottom-right; clicking the row still opened the case detail screen correctly. Console clean apart from the usual harmless Firestore multi-tab warning.
  - Current live version: `docs/test/` at `0.4.1-t07`. Root untouched at `0.4.2`.
- 2026-09-19 — **Cases list follow-up, `docs/test/` only.** Title line kept as-is; bottom line rearranged — left side is now a single "worker | day N | due date" line (was a stacked day/due block with "status | worker" on the right), right side is just the stage name alone (e.g. "New"). Verified live: "work | day 1 | 2026-10-17" on the left, "New" alone on the right.
  - Current live version: `docs/test/` at `0.4.1-t08`. Root untouched at `0.4.2`.
- 2026-09-19 — **Case detail Info section trimmed, tags folded into status, at the user's request, `docs/test/` only.** Info's compact (read-only) view now shows only "Client" (org — name); dropped case number/client case number/name/start date/case type/case manager/opened-by/due-date and the on-hold/high-priority/show-research checklist entirely, since the first group duplicates what the case title already shows and the toggles are now represented differently (below). **"Edit info" is unchanged** — the full form (every field, every toggle) still works exactly as before; only the collapsed read-only summary shrank.
  - **New `effectiveStatusText(c)` helper** (in `cases.js`): on-hold or "in research" (`showResearchToClient`) now *replace* the status text itself wherever it's shown (case title, cases-list row) — e.g. "On hold" instead of "New"/"Lab"/etc. — rather than appearing as separate badges/checkmarks. On hold wins if a case is somehow both on hold and flagged for research. High priority doesn't replace the status; it prefixes a "★ " star, and on the cases-list row specifically also gets a bold red treatment (`.status-priority`, reusing the existing `--err` color already used for priority badges elsewhere). Removed the now-redundant on-hold/priority badges that used to sit on the cases-list row's title line, since they'd otherwise duplicate the new status text.
  - **Verified live via browser automation**: compact Info view shows just "Client: Hak — Hok1" plus the Edit info button; clicking Edit info still opens the full form with all fields and all three toggles intact; checking "High priority" and reopening the case showed "★ New" in both the case title and the cases-list row (the list row rendered in bold red); toggle reverted back off afterward to leave the test data as found. Console clean apart from the usual harmless Firestore multi-tab warning.
  - Current live version: `docs/test/` at `0.4.1-t09`. Root untouched at `0.4.2`.
- 2026-09-19 — **Samples add flow redesigned at the user's request, `docs/test/` only: batch add-view instead of an always-open inline form.** The Samples section now shows a "+ Add" toggle (same open/close pattern as "+ New case") instead of a permanently-visible 3-field form. Opening it reveals: Item name, Number of copies (default 1), a repeatable "+ Add sample" list of named sample rows, and a repeatable "+ Set zone" list of named zone rows -- replacing the old comma-separated zones text field entirely, per the user's explicit "don't use the text comma way." New shared helper `buildNameListEditor()` (single-input-per-row, reuses `.value-row`'s existing flex/gap styling built for the action value editors) backs both lists.
  - **Creation logic**: if the sample-name list is empty, the item itself is treated as one sample per copy (`item: null`, `name: <item name>` -- no separate grouping label makes sense when there's no sub-part). If sample names are listed, each gets created `copies` times with `item: <item name>` as the shared grouping label. Names get a `" (n)"` suffix only when `copies > 1`, so the common single-copy case stays clean. Requires either an item name or at least one listed sample (shows an inline error otherwise). Each created sample still gets seeded with the case's default lab-workflow actions, same as before.
  - **Verified live via browser automation**: opened the add-view, entered item name "Bearing 1", added sample name "Ball", added zone "Core", set copies to 2, clicked Create -- correctly created "Bearing 1 - Ball (1)" and "Bearing 1 - Ball (2)", each showing "Zones: Core" when expanded. Console clean apart from the usual harmless Firestore multi-tab warning. **Left the two test samples in place** -- there's no delete-sample feature in the UI yet (only whole-case deletion and per-action reopen exist), so they can't be cleaned up from the app; harmless test artifacts named clearly enough (Bearing 1 - Ball) to be obviously mine if the user wants to remove them via the Firebase Console.
  - Current live version: `docs/test/` at `0.4.1-t10`. Root untouched at `0.4.2`.
- 2026-09-19 — **Sample deletion + per-sample zones, at the user's direct follow-up request, `docs/test/` only.** Each expanded sample card now has two new controls:
  - **"Delete sample"** (team_leader only, matching the existing unchanged `samples` delete rule in `setup/firestore.rules`) -- same two-step inline confirm pattern already used for case/user deletion. Cascade-deletes the sample's own `actions` subcollection first, same reasoning as case deletion (Firestore doesn't cascade subcollections on its own).
  - **"+ Zone"** -- appends a new zone to that specific, already-existing sample's `zones` array directly, via a small inline input + Add/Cancel, rather than only being settable at batch-creation time in the Add view (which is unchanged and still available for setting initial zones on a new batch).
  - **Cleaned up my own leftover test data from the previous entry**: last time I couldn't delete the "Bearing 1 - Ball (1)/(2)" samples I'd created to verify the batch add-view, since no delete feature existed yet. Now that it does, I used it to remove them as part of verifying this feature. Left several other pre-existing samples in the same case alone (e.g. "Bearingg - Ball/Or", "Samp 1", "Screw") since they don't match anything I created and predate this session.
  - **Verified live via browser automation**: expanded a sample, used "+ Zone" to add "Surface" to an existing "Core" zone (correctly showed "Zones: Core, Surface"), then used "Delete sample" (confirm/cancel row appeared correctly) to remove it -- the workflow section's sample count dropped accordingly each time (1/10 → 1/9 → 1/8). Console clean apart from the usual harmless Firestore multi-tab warning.
  - Current live version: `docs/test/` at `0.4.1-t11`. Root untouched at `0.4.2`.
- 2026-09-22 — **Case-view header rework + case deletion from the overview, at the user's direct request, `docs/test/` only.** No TASK.md driving this (current TASK.md is the unrelated Firestore-rules-deploy tooling task) -- worked directly off the user's own message. Changes:
  - **Notes and Edit info are now header icon buttons** instead of text buttons: `toggleNotesBtn` swapped its "Notes"/"Back to case" text for an icon (title/aria-label still change for accessibility); a new `editInfoBtn` icon (pencil) replaced the "Edit info" text button that used to live inline atop the Info section, and now sits in the case-detail header next to Notes. The Info section's own "Info" heading + button line is gone entirely -- the section starts straight into its content (compact `Client` row, or the full edit grid when `editInfoBtn` is active).
  - **Case deletion moved into the header too**: a red trash icon (`deleteCaseBtn`, `.icon-btn-danger`) next to the edit icon replaces the old plain-text "Delete case" button that sat at the bottom of the workflow section. Same cascade-delete function, same team_leader-only gating (both via the button's own hidden/shown state and the unchanged `setup/firestore.rules` delete rule) -- only where the control lives changed, not who can use it or what it does.
  - **New: delete icon directly on each case row in the overview** (`renderCaseRow`/`buildCaseRowDeleteControl`), team_leader-only, same cascade delete + confirm-before-destroy pattern as the header/sample versions -- lets a team leader remove a case without opening it first. Implemented as an absolutely-positioned inline confirm popover anchored under the icon; the whole delete-control wrapper swallows its own clicks (`stopPropagation`) so it never triggers the row's own "open this case" handler.
  - **Case header text reformatted** per the user's literal spec -- `title(case/client num, name). day, date, status. client(org-name)` -- read as: `{onameOf}. day {n}, {dueDate}, {status}. {clientOrgName}`. New `caseHeaderText()` replaces the old `caseTitle()`; new `clientOrgNameOf()` returns just the org name (no contact name) for this line, distinct from the existing `clientDisplayText()` (org — client) still used in the compact Info row. **Deliberate scope call**: the old header also showed case manager (`c.caseManager || 'Unassigned'`) between the title and day count -- the user's format has no slot for it, so it's dropped from the header entirely. It's not lost data -- still visible/editable via the full Info edit grid -- just no longer summarized up top. Flagging this in case it was meant to stay and got missed rather than intentionally cut.
  - Bumped `docs/test/` straight through to `0.4.1-t12` for this (still the same 0.4.1 line already in progress, not a new feature bump) -- every `?v=` reference and `APP_VERSION` updated together via a repo-wide find/replace across `docs/test/`, same mechanical bump pattern as prior `-tNN` increments.
  - Added a `docs/test/`-only changelog note under the existing `0.4.1` entry (not a new entry -- this build hasn't promoted/relaunched, so it's still additional notes on the same in-progress version, matching how root's changelog only gained its own `0.4.2` entry once that whole line actually launched).
  - **Verified live via real browser automation against `docs/test/`** (never root, per CLAUDE.md), using the already-authenticated `lead`/team_leader session already open in the browser (no password entered): confirmed the overview's per-row trash icon renders red and its confirm/cancel popover works without navigating into the case; opened a case and confirmed the header now shows all three icons, the edit icon toggles the full edit grid and highlights while active, the notes icon swaps to the Notes view and back, and the header delete icon's confirm bar appears/cancels correctly under the header without deleting anything. Left both of the user's existing real test cases untouched -- cancelled every delete confirm rather than completing one, since neither case is test data I created. Console clean, no errors.
  - Current live version: `docs/test/` at `0.4.1-t12`. Root untouched at `0.4.2`.
- 2026-09-22 — **Follow-up correction on the same day's header work, `docs/test/` only, at the user's direct correction.** Two fixes to the previous entry's build:
  - **Header was wrong**: the user clarified the "."s in their original shorthand (`title(...). day, date, status. client(org-name)`) meant line breaks, not literal text joined onto one line -- so `caseHeaderText()` (single string, period-joined) is replaced with `caseHeaderLines()` (returns an array of 3 lines), rendered as three separate `<div>`s inside `#caseDetailTitle` (changed from a `<p>` to a plain `<div>` in `index.html`, since a `<p>` can't validly contain block-level children). Each line is set via `.textContent`, not `innerHTML`, so case/client names with special characters can't break the markup.
  - **Third line also changed**: the user said drop the word "Client" and show `clientorg-client` instead of just the org name -- so the line now reuses the existing `clientDisplayText()` (org — client, already used in the compact Info row) instead of the org-only `clientOrgNameOf()` from the previous entry, which is now deleted (dead code, no longer referenced anywhere).
  - **Icon outline**: `toggleNotesBtn` and `editInfoBtn` both gained a new `.icon-btn-accent` class (`color`/`border-color: var(--accent)`, the app's orange) so they read as visually distinct/branded buttons at rest, not just on hover or while active -- same pattern as the red `.icon-btn-danger` already used for delete, just the accent color instead.
  - Bumped to `0.4.1-t13`, same mechanical repo-wide find/replace as before.
  - **Not independently verified live this time** -- the user said not to spend time testing, they'll test it themselves. Pushed to `docs/test/` and confirmed the deploy landed (polled the raw served `firebase-init.js` until it returned `0.4.1-t13`), but no browser-automation pass was run against the UI itself. Flagging this explicitly since every other entry in this file up to now included a live-verification pass -- this one doesn't, by the user's own request, not because it was skipped by oversight.
  - Current live version: `docs/test/` at `0.4.1-t13`. Root untouched at `0.4.2`.
- 2026-09-22 — **Distinctive visual-identity pass, at the user's request after installing the official `frontend-design` Claude Code plugin, `docs/test/` only.** User explicitly scoped it via a direct question first: "distinctive pass, keep current palette" over a full overhaul or single-screen pilot. Followed that plugin's own mandated process (plan a compact token system, review it against the brief for genericness, then build) rather than just reskinning defaults:
  - **Typography**: added IBM Plex Sans (`--font-sans`) app-wide, replacing the plain system-font stack, and IBM Plex Mono (`--font-mono`, `.mono` utility class) reserved specifically for readout-style data -- case/client number inputs, measurement value/units inputs (`buildValueListEditor`), the case-detail header's day/date/status line, the version badge, and changelog version/date text -- not applied as blanket "technical" decoration elsewhere (prose, names, labels stay in Plex Sans). Loaded via a Google Fonts `<link>` (no build step, matches the project's "plain HTML/CSS/JS" stack); the CSS fallback stack keeps the previous system fonts if the network font fails to load.
  - **Corners**: replaced the previously scattered 6px/8px/10px/14px `border-radius` values across `.btn`/`.icon-btn`/inputs/`.card`/`.sample-card`/`.add-samples-view`/`.note-item`/`.badge`/etc. with one `--radius:3px` token everywhere -- sharper, more "instrument panel/spec sheet" than the soft-rounded default.
  - **Status as a stamped tag**: `.case-row-status` (overview list) changed from plain right-aligned text to a small outlined, uppercase, mono box (border + padding, no fill) -- reads like an inspection/evidence tag rather than a notification badge. `.badge-onhold`/`.badge-priority` (client-facing view, `client-view.js`) changed the same way, from filled colored pills to outlined mono tags in the same warn/err colors, for one consistent status language across both the team and client-facing screens.
  - **Palette untouched** -- every existing color token (`--bg`, `--panel`, `--accent`, etc.) is exactly what it was; this pass spent its "boldness" on type + structure only, per the plugin's own restraint guidance.
  - Bumped to `0.4.1-t14`, same mechanical repo-wide find/replace as prior `-tNN` bumps. Added a changelog note under the existing `0.4.1` entry (same in-progress-line reasoning as prior entries).
  - **Not independently verified live** -- per the user's standing instruction from earlier the same day ("don't waste time on testings. i will test myself"), no browser-automation pass was run. Only confirmed the deploy itself landed (polled the raw served `firebase-init.js` until it returned `0.4.1-t14`). Worth a close look once tested: the corner-radius drop is the biggest visual delta in this pass (14px→3px on the main `.card` container is a real departure from how the app has looked since Iteration 1), and the mono font on narrow inputs (case number, measurement value/units) is worth checking at actual field widths in case IBM Plex Mono's character width causes any clipping the previous system font didn't.
  - Current live version: `docs/test/` at `0.4.1-t14`. Root untouched at `0.4.2`.
- 2026-09-22 — **Case workflow as named clickable stage circles, + dropped the now-redundant "Client" info row, `docs/test/` only, at the user's direct request.** The original ask ("allow the ability to add actions to case workflow, show case workflow as numbered circle progress flow") was genuinely ambiguous on the data-model question, so I asked two clarifying questions rather than guess on a live app's core stage machine (per `CLAUDE.md`'s "don't guess silently on data model shape" instruction). Real answers, not what either of my multiple-choice options predicted verbatim:
  - The user distinguished "case workflow" (the top-level `new → lab → write → archive → done` stage machine) from the other named workflows already in the model (lab/samples, writing, archiving) -- "add actions" turned out to mean: represent those stages as clickable nodes, where clicking one (e.g. "Lab") opens that stage's own view. Not a request to let users insert arbitrary custom stages into the sequence, and not a separate case-wide to-do list -- I'd offered both as options and neither was picked.
  - "Numbered circles" became **named** circles ("New"/"Lab"/"Write"/"Archive"/"Done" inside each circle, not "1/2/3/4/5") -- and clicking one previews that stage's panel, confirming the "circles as a header strip, one clickable per stage" reading over the two other options I'd offered (replacing the whole section's interaction model, or just the text heading).
  - **Built as a pure UI layer, no schema change**: new module-level `viewedStage` (defaults to the case's real `stage` on every case open, same transient-UI-state pattern as `infoEditMode`/`expandedSamples`/`notesShown`). Clicking a stage-flow circle sets it and re-renders. Crucially, `viewedStage` only controls which panel *displays* -- the actual interactive controls (Start Lab, the writing-review buttons, the archiving checklist's add/remove/mark-done) only render when the viewed stage matches the case's real `stage`; any other circle shows a read-only preview (reusing `buildArchivingWorkflow(c, false)` for archive, since it already fully suppresses its own buttons when `editAllowed` is false -- no new function needed there). This was a deliberate safety call I made rather than asked: without it, a team leader could click back to an already-passed stage and re-trigger a transition or button meant for a different point in the case's history. Flagging in case the user actually wants those controls live everywhere, not just read-only previews off the current stage.
  - Visual: circles are true `border-radius:50%` -- the one deliberately circular element in an otherwise all-sharp-cornered app (post last session's `--radius:3px` pass) -- reserved for this one lifecycle-flow device rather than reused elsewhere, per the `frontend-design` plugin's "spend your boldness in one place" guidance from the prior session. Connected by a horizontal line (accent-dim where the case has already passed that stage, plain border-soft ahead of it); current stage filled solid accent; whichever stage is being *viewed* gets an accent ring instead of a fill so "current" and "being previewed" stay visually distinct even when they're different stages.
  - **Also removed**: the compact Info section's standalone "Client" row (`infoRow('Client', clientDisplayText(c))`) -- redundant now that the case header's own third line already shows the same org/client pairing (from the header rework two entries back). `buildInfoSection` now returns `null` in compact mode instead of an empty section; the call site in `renderCaseDetail` guards the `appendChild` on that. The now-fully-unused `infoRow()` helper was deleted rather than left dead.
  - Bumped to `0.4.1-t15`, same mechanical version bump as before. Added two changelog bullets under the existing `0.4.1` entry.
  - **Not independently verified live** -- same standing instruction as the last two entries (the user tests `docs/test/` themselves). Only confirmed the deploy landed (polled `firebase-init.js` for `0.4.1-t15`). Worth checking in particular: whether read-only previews of past/future stages are actually the behavior wanted (see the flagged judgment call above), and whether 5 circles + connecting lines fit comfortably on a phone-width screen (`.stage-flow` has `overflow-x:auto` as a safety net, but wasn't checked against an actual narrow viewport).
  - Current live version: `docs/test/` at `0.4.1-t15`. Root untouched at `0.4.2`.
